<%= form_for(feedstock) do |f| %>
  <% if feedstock.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(feedstock.errors.count, "error") %> prohibited this feedstock from being saved:</h2>

      <ul>
      <% feedstock.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :biomass_type_id %>
    <%= f.text_field :biomass_type_id %>
  </div>

  <div class="field">
    <%= f.label :harvest_id %>
    <%= f.text_field :harvest_id %>
  </div>

  <div class="field">
    <%= f.label :amount %>
    <%= f.text_field :amount %>
  </div>

  <div class="field">
    <%= f.label :unit %>
    <%= f.text_field :unit %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
