<%= form_for(biomass) do |f| %>
  <% if biomass.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(biomass.errors.count, "error") %> prohibited this biomass from being saved:</h2>

      <ul>
      <% biomass.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
